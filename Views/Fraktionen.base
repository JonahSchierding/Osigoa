views:
  - type: table
    name: Table
    filters:
      and:
        - Type == "Fraktion"
        - file.basename != "Fraktion"
    order:
      - file.name
      - Type
    sort:
      - property: file.name
        direction: ASC
