views:
  - type: table
    name: Table
    filters:
      and:
        - Type == "Player"
        - file.basename != "Player"
    order:
      - file.name
      - Type
    sort:
      - property: file.name
        direction: ASC
