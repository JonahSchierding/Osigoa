views:
  - type: table
    name: Table
    filters:
      and:
        - file.hasProperty("Type")
        - Type == "Ort"
        - file.basename != "Ort"
    order:
      - file.name
      - Type
    sort:
      - property: file.name
        direction: ASC
      - property: Type
        direction: DESC
